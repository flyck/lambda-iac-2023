{
  "name": "bun-lambda",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "build": "cd ../hello-world; bun run build; zip bundle.zip dist/bundle.js",
    "deploy": "sls deploy --stage dev"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "serverless": "^3.34.0",
    "serverless-offline": "^12.0.4"
  }
}
